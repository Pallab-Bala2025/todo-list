import  ToDoList from './ToDo-List.jsx';

function App(){
  // This is the main App component that imports and renders the ToDoList component
  // It serves as the entry point for the application
  // This is the main App component that renders the ToDoList component
  // You can add more components or logic here as needed
  // For now, it simply returns the ToDoList component

  return (<ToDoList />)

}

export default App;